<?xml version="1.0" encoding="UTF-8"?>
<blueprint
        xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
        http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd
    ">

    <camelContext xmlns="http://camel.apache.org/schema/blueprint">
        <onException>
            <exception>java.lang.Exception</exception>
            <to uri="mock:exception"/>
        </onException>

        <route>
            <from uri="direct:start" />
            <to uri="osgi:multicast:consumer?aggregationStrategy=#aggregationStrategy&amp;stopOnException=true" />
            <split>
                <simple>${property.CamelGroupedExchange}</simple>
                <setBody>
                    <simple>${body.getIn().getBody()}</simple>
                </setBody>
                <to uri="mock:finish" />
            </split>
        </route>
    </camelContext>

    <bean id="aggregationStrategy" class="org.apache.camel.processor.aggregate.GroupedExchangeAggregationStrategy" />

</blueprint>